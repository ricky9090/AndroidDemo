<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rickyxe.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="rickyxe.demo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="rickyxe.demo.bgwork.BackgroundWorkActivity" />

        <service android:name="rickyxe.demo.bgwork.BackgroundService" />

        <activity android:name="rickyxe.demo.bgwork2.BackgroundWorkActivity2" />
        <service android:name="rickyxe.demo.bgwork2.BackgroundService2" />

        <activity android:name="rickyxe.demo.dialogexample.DialogTestActivity" />

        <activity android:name="rickyxe.demo.contactpick.ContactPickActivity" />

    </application>

</manifest>